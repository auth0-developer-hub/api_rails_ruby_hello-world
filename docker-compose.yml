version: "3.8"
services:
  api:
    build: .
    image: api_rails_ruby_hello-world_starter
    container_name: api_rails_ruby_hello-world_starter
    ports:
      - "6060:6060"
    env_file:
      - .env
